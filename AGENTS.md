# AGENTS.md

## 工程目标

本项目用于构建和维护一组基于以下本体论的提示词：

> 概念是对具体经验中的结构的指认和命名。

项目中的提示词应当围绕这一过程组织模型行为：先处理具体经验，从中形成可以被辨认的结构，再把该结构明确指认出来，最后进行命名。

项目当前首先维护两类提示词：

- `prompts/teaching.md`：用于已有概念的教学。
- `prompts/concept-creation.md`：用于从经验中形成和命名新的概念。

本项目研究的是这种概念形成方法本身。具体学科知识只作为经验材料，不构成项目维护的知识库。

## 项目结构

```text
.
├── AGENTS.md
└── prompts/
    ├── teaching.md
    └── concept-creation.md
```

`AGENTS.md` 保存项目共同采用的本体论、项目特有概念和文件职责。

`prompts/teaching.md` 保存教学提示词。

`prompts/concept-creation.md` 保存概念创造提示词。

## 项目概念

后面的规则需要使用几个日常语言无法准确区分的概念。这里先通过材料形成这些概念，再进行指认和命名。

### 经验材料

#### 材料

编程时，程序员可以直接在文件中看到：

```rust
fn main() {
    let var1 = 1;

    var1 = 2;
}
```

实际编译后，可以直接看到：

```text
error[E0384]: cannot assign twice to immutable variable `var1`

help: consider making this binding mutable
```

这些内容可以改用自然语言描述：

> 文件中先出现 `let var1 = 1;`，后面出现 `var1 = 2;`。编译时出现 E0384 错误，错误信息写着不能再次给不可变变量 `var1` 赋值，并建议把这个绑定改成 mutable。

---

处理数学问题时，可以直接看到并处理：

\[
f_n(x)=x^n,\qquad x\in[0,1].
\]

固定 \(x<1\) 时有 \(x^n\to0\)；在 \(x=1\) 时始终有 \(f_n(1)=1\)；取

\[
x_n=1-\frac1n
\]

时，有

\[
f_n(x_n)=\left(1-\frac1n\right)^n\to e^{-1}.
\]

这些内容可以改用自然语言描述：

> 材料给出了函数列 \(x^n\)，定义域是从 0 到 1。对于固定的小于 1 的 \(x\)，函数值趋近于 0；在 1 处函数值一直等于 1；另外又选择了一列越来越接近 1 的 \(x_n\)，相应函数值趋近于 \(e^{-1}\)。

---

讨论哲学问题时，可以直接阅读：

> Smith 有充分证据相信 Jones 会得到一份工作，也知道 Jones 口袋里有十枚硬币。Smith 因此相信“得到这份工作的人口袋里有十枚硬币”。最后得到工作的是 Smith，而 Smith 此前不知道自己口袋里也恰好有十枚硬币。

这些内容可以改用另一段话描述：

> Smith 根据有关 Jones 的证据形成了一个关于“得到工作的人”的信念。后来工作由 Smith 得到，而 Smith 本人也有十枚硬币，所以这个信念最后为真。

#### 指认

每组前面的内容，都是人在相应活动中实际直接经历的内容。代码是在编程时直接看到的；编译器输出是在编译后直接看到的；数学表达式和计算是在处理数学问题时直接面对的；哲学案例是在讨论这个问题时实际阅读的材料。

每组后面的文字谈论的仍然是同样的内容，但原来的材料已经被语言描述所替代。即使描述完整、准确，也没有再次提供原来的代码、编译器输出、数学表达式或者案例本身。

#### 命名

后文把人在相应实践中直接经历的这些内容称为**经验材料**。

---

### 目标结构

#### 材料

继续使用前面的程序问题：

```rust
let var1 = 1;
var1 = 2;
```

```rust
let mut var1 = 1;
var1 = 2;
```

```rust
struct Type1 {
    field1: i32,
}

let mut var1 = Type1 { field1: 1 };
var1.field1 = 2;
```

在这些材料中，可以不断改变具体代码，但当前任务可以始终关注同一个部分：建立一个绑定时是否有 `mut`，会怎样影响以后通过这个绑定修改相应内容。

---

前面的数学材料也可以继续变化。

可以考察 \(x^n\) 在 \([0,1]\) 上的行为，也可以把定义域限制到 \([0,a]\)，其中 \(a<1\)；还可以继续改变函数列。

材料可以变化，但当前任务可以始终关注“每个固定位置分别满足某个条件”和“存在一个同时适用于所有位置的条件”之间的区别。

---

哲学案例也可以更换人物、信念内容和事情经过。

当前任务可以始终关注信念所依据的情况、实际使命题为真的情况以及二者之间的关系。

#### 指认

一份材料中同时存在许多可以注意和抽象的东西。当前任务只选择其中一部分，让这一部分在不同材料中反复出现，并希望以后在新的材料中仍然能够认出来。

程序中的具体数字、数学中的字母名称、哲学案例中的人物姓名都可以变化，而当前正在形成的认识仍然可以保持。

#### 命名

后文把希望通过一组材料形成、并能够迁移到新材料中的这部分称为**目标结构**。

---

### 具体形式、结构变式与无关实例化内容

#### 材料

程序中：

```rust
let mut var1 = 1;
var1 = 2;
```

把它改成：

```rust
let mut number = 1;
number = 2;
```

这里只更换了名称。

把 `1` 和 `2` 换成其他具体整数，也没有使当前目标结构出现新的形态。

但是改成：

```rust
struct Type1 {
    field1: i32,
}

let mut var1 = Type1 { field1: 1 };
var1.field1 = 2;
```

时，需要形成的关系仍然存在，但修改已经通过一个复合值的组成部分发生。

如果函数名称与当前目标结构无关，可以直接使用 `fun1`。如果某段代码只需要保留“使用了 `var1`”这一点，可以直接写成：

```text
<使用了 var1 的代码>
```

而不具体规定它完成什么业务功能。

---

数学材料中，把 \(x\) 改名成 \(y\)，或者把一个对当前问题没有作用的具体常数换掉，不会形成新的认识材料。

但从“固定一个位置以后考察 \(n\) 增大”变成“取值位置本身随着 \(n\) 改变”，会使同一个问题以另一种实际形态出现。

---

哲学材料中，把 Smith 和 Jones 换成另外两个人，把工作换成另一件事情，只会增加新的故事内容。

如果改变的是信念根据与实际使命题为真的情况之间的关系，那么正在研究的结构就以另一种形态出现。

#### 指认

材料中的一些具体内容只决定“这个实例具体是什么”。替换它们以后，使用者面对的目标结构没有发生有意义的变化。这些内容如果没有作用，可以直接保持一般化。

另一类变化仍然保留同一个目标结构，但使这个结构实际出现的方式发生变化。这种变化能够提供新的抽象材料。

目标结构在每一份这样的材料中都会实际出现一次，只是出现方式不同。

#### 命名

后文把目标结构在一份材料中的实际呈现称为它的一种**具体形式**。

把保持目标结构不变、同时使其呈现方式发生变化的材料变化称为**结构变式**。

把与目标结构无关、只规定某个实例具体是什么的内容称为**无关实例化内容**。

---

### 指认

#### 材料

经过前面的程序材料以后，可以说：

> 前面的几组代码虽然具体写法不同，但都在处理同一个关系：建立绑定时的 `mut` 与随后能否通过这个绑定修改相应内容之间的关系。

经过数学材料以后，可以说：

> 前面的计算不断出现同一个区别：可以对每一个固定位置分别选择满足要求的阶段，与能够选择一个阶段同时控制全部位置，并不是同一件事。

经过哲学材料以后，可以说：

> 前面的案例反复出现的是：形成信念所依据的情况，与实际使命题为真的情况，可以具有不同关系。

#### 指认

这些话没有提供新的实例，也没有给正在讨论的东西增加一个此前没有经历过的性质。

它们把此前分别存在于多份材料中的那部分共同内容单独指出，使使用者明确知道自己刚才反复经历的是哪一个部分。

#### 命名

后文把这种把已经从材料中形成、但尚未被单独说出的内容明确指出来的工作称为**指认**。

---

### 命名

#### 材料

前面的程序材料和指认完成以后，可以进一步告诉使用者：

> `let mut` 中的这种性质在 Rust 中称为 mutability；相应的绑定称为 mutable binding。

前面的数学材料和指认完成以后，可以进一步告诉使用者：

> 前面两种要求分别对应 pointwise convergence 和 uniform convergence。

前面的哲学材料和指认完成以后，可以进一步告诉使用者：

> 前面的这种案例通常称为 Gettier case。

在这些名称出现以前，相关结构已经通过材料形成，并且已经被明确指出。名称出现以后，可以把以后遇到的术语重新接回前面的经验。

#### 指认

这里增加的内容主要是公共知识体系已经采用的名称、记号或其他正式表达。它们给已经形成并被指认的认识建立稳定的表达方式。

#### 命名

后文把这项工作称为**命名**。

## 整体架构

当前项目是提示词定义仓库，没有运行时程序。

`AGENTS.md` 保存所有提示词共同依赖的本体论、项目特有概念和项目结构。

`prompts/teaching.md` 与 `prompts/concept-creation.md` 分别定义两种任务的具体方法。只适用于某一种任务的概念和规则应保留在对应提示词中，不进入 `AGENTS.md`。
